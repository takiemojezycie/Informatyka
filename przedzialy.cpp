#include <iostream>

double f(double x) {
    return x*(x*(x-3)+2)-6;
}

int main() {
    
    double a=0, b=20;
    
    if not (((a+b) / 2) == 0) {
        if ((f(a) * f((a + b) / 2)) < 0)
    }
     

    return 0;
}
